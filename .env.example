# Desarrollo: direcciones accesibles desde el equipo que ejecuta ng serve.
GATEWAY_URL="URL del gateway"
AUTH_URL="URL de autenticación"

# Docker Desktop: direcciones accesibles desde el contenedor Nginx.
DOCKER_GATEWAY_URL="URL del docker gateway"
DOCKER_AUTH_URL="URL de autenticación en Docker"
FRONTEND_PORT="Puerto del frontend"
